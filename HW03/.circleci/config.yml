version: 2.1
jobs:
  build:
    docker:
      - image: cimg/python:3.14.2  # Use the Python version you're developing in
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install requests
      - run:
          name: Run tests
          command: python -m unittest test_github_api.py